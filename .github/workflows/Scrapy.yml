name: Scrapy CI

on:
  push:
    branches: ["main"]
    paths:
      - 'spiders/**'   # Adicionado para garantir que mudanças na pasta spiders disparem o workflow
      - 'BackEnd/**'
  pull_request: 
    branches: ["main"]
    paths:
      - 'spiders/**'
      - 'BackEnd/**'

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: spiders # CORREÇÃO: Adicionado hífen (era 'working directory')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install pytest scrapy # Garante que scrapy esteja instalado para os testes

      - name: Run tests
        run: |
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          pytest